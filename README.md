# app-megabox

Reconstrução do app Bubble `grupomegabox` (Grupo MegaBox) em Next.js + Supabase + Vercel, tudo em São Paulo.

Comece por `CLAUDE.md` (regras) e `mapa/LEIA-ME.md` (como ler o app decompilado).

Estado (24/09/2026): mapa do Bubble completo em `mapa/`; specs funcionais em produção em `specs/paginas/`; projeto Supabase `megabox` criado, sem tabelas; app não iniciado.
